things to clean up if choosing prototype 2:

- logo assets thrown into /assets/logos regardless of size or resolution 

- page hero styles are not uniform (news & contract-vehicle pages are defined by layout, others are defined by blocks within the page frontmatter)